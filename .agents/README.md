# Agent context for dcmm-desktop

Start with **[INSTRUCTIONS.md](INSTRUCTIONS.md)**. That file is the full brief for a new session.

This repo is the **macOS-only** DeepCleanMyMac app. All scan/clean logic lives in **dcmmlib** (git submodule).
